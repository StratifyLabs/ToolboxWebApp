import React from 'react'
import { Card, Form, Button } from 'react-bootstrap'



const Debug = props => {
  return (
    <pre>
     Show the core dump, give buttons for Reset, Halt, Run, Etc
    </pre>
  )
}

export default Debug
